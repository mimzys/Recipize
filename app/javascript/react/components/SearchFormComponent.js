import React from 'react';
import { browserHistory, Link } from 'react-router';

const SearchFormComponent = (props) => {
  return(
    <h1>Hello from SearchFormComponent!</h1>
  )
}

export default SearchFormComponent;
